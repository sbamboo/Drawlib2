Through the coloring module you can have customTags given to a TextObj, this allows for variables
these are called Tags.

In the DrawlibV1 assets format you could have variables in the texture/sprite you can here aswell but they are stored in tokens.
Which are procent enclosed strings.